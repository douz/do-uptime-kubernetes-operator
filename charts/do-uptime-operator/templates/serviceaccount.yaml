{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "do-uptime-operator.serviceAccountName" . }}
  labels:
    {{- include "do-uptime-operator.labels" . | nindent 4 }}
{{- end }}
